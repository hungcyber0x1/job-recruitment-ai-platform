/**
 * Job Model Schema — see migration 005_create_jobs_table.sql
 *
 * id, employer_id, category_id, title, description, requirements, benefits,
 * salary_min, salary_max, currency, location, experience_required,
 * education_required, type, status, deadline, views, created_at, updated_at
 */

module.exports = {}; // Exporting as placeholder for schema reference
