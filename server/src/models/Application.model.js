/**
 * Application Model Schema — see migration 006_create_applications_table.sql
 *
 * id, candidate_id, job_id, resume_url, cover_letter, status, applied_at
 */

module.exports = {}; // Exporting as placeholder for schema reference
