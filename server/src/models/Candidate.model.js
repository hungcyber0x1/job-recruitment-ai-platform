/**
 * Candidate Model Schema — see migration 002_create_candidates_table.sql
 *
 * id, user_id, bio, experience_years, current_job_title,
 * education_level, education (JSON), experience (JSON), location,
 * resume_url, phone, created_at, updated_at
 */

module.exports = {}; // Exporting as placeholder for schema reference
