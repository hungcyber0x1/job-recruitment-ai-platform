/**
 * ChatMessage Model Schema — see migration 011_create_chat_messages_table.sql
 *
 * id, sender_id, conversation_id, message, is_ai,
 * attachment_url, attachment_type, created_at
 */

module.exports = {}; // Exporting as placeholder for schema reference
